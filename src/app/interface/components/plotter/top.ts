import { BaseComponent } from 'src/app/interface/components/blocks/base-component';

export class TopComponent extends BaseComponent {
    constructor() {
        super({ tag: 'div', id: 'mcs-plotter-top' });
    }
}
